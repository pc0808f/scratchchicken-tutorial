# 【更新回覆】依實際核對與處理結果（2026-09-13）

> 本區塊是對下方原始報告的核對與更正。與原始報告衝突之處，**以本區塊為準**。

## A. 核對結論修正

1. **老鼠移動的「正確方法」認定更正。**
   課綱「顏色碰撞法」的真正做法是：**紅藍標做在老鼠角色身上（像自帶的兩個感應器），老鼠用身上的紅藍標壓在路線上來判斷方向**。這對齊課綱決策1 與第 178 行「老鼠**紅藍造型**」。
   - 因此原始報告在「W4 建議」中提出的「在道路轉角放紅藍**標記**、老鼠用『碰到顏色』感應」是**另一種機制、方向被帶偏，不採用**。
   - 目前 W4 截圖（`造型換成藍/紅框 → 碰到 路徑角色(Sprite1) → 轉`）本來就是「自帶感應器法」，**是對的**，不需要改成道路標記，也不需要重拍。

2. **W3「截圖已埋入 Sprite1 轉彎做法」的說法不成立（已過時）。**
   目前 `week3/index.md` 主流程乾淨，只有直線前進；紅藍造型製作放在附錄 REFERENCE A／B，屬於課綱規定的 W3 內容（老鼠紅藍造型）。**W3 本來就正確，維持原樣。**

3. **頁面文字先前已大致對齊課綱，真正落差是「用詞」與「W5 截圖」。**
   近期 commit 已把 W4／W5 的程式卡片對齊課綱（W5 已含「如果造型=蛋」條件文字）。原始報告把文字整體說成待修，部分過時。實際要處理的是：W4 文字用詞與截圖積木不一致（已修），以及 W5 截圖本身缺條件（待辦）。

## B. 已定案的教學決策

- **移動機制**：老鼠自帶紅藍標感應器，壓路線判斷方向。
- **轉彎邏輯**：藍標壓到路線 → 往右修正；紅標壓到路線 → 往左修正。
- **W3＋W4 是同一件事**（讓老鼠跟著紅藍走），**直線走是中繼點、不是終點**。
- **開場示範放在 W3**：老師先按綠旗放完成版 demo，讓學生先看到目標。

## C. 已完成的修改（本次只動文字，未動圖）

**W3（`week3/index.md`）**
- 學習目標頁新增「老師先示範」框，並把 W3＋W4 框成同一件事、直線走定位為中繼點。
- STEP 02A 補充：紅框、藍框「就是老鼠的感應器」。
- 下週預告改為「用老鼠身上的紅藍標壓路線來轉彎」。

**W4（`week4/index.md`）**
- 封面、學習目標清單、STEP 01 概念、STEP 02／03 的標題／說明／程式卡片、圖說、頁尾、下課檢查清單、PLUS 挑戰——全部由「碰到藍色／紅色／顏色」改為「**紅藍標壓路線**」的講法。
- 程式卡片改為與截圖一致：`造型換成 藍/紅框 → 如果〈碰到 路徑角色〉→ 向右/左轉`。
- 已確認全頁不再出現舊的「碰到藍色／紅色／顏色」用詞。

## D. 尚未處理（待辦）

1. **W5 截圖**：`week5/resources/turret/開始是蛋點了變母雞並說出話.jpg` 仍缺「如果造型=蛋」條件（頁面程式卡片文字已含條件）。需重拍或補一張正確截圖。
2. **W4 抵達飼料桶要停止**：目前只「隱藏」，建議加「停止這個程式」（等 W8 改分身後再改成刪除分身）。此點原始報告仍成立。
3. **命名對齊**：路徑角色在截圖裡叫 `Sprite1`，教材寫「路徑角色（畫面上預設叫 Sprite1）」。若要完全一致，可在專案內把該角色改名為「路徑角色」。

---

# Week 3～Week 5 教材核對與修改報告（原始版本）

> 註：下方為最初版本，部分結論（特別是 W3 的 Sprite1 說法、以及 W4 改用「道路顏色標記」的建議）已於上方【更新回覆】更正，請以上方為準。

## 一、報告目的

本報告依新版 `課綱.md`，核對 `week3/index.md`、`week4/index.md`、`week5/index.md`，整理每週：

- 原本應該教什麼
- 目前教材實際呈現什麼
- 建議如何修改
- 修改時需要準備哪些素材

本報告以課綱為最高標準。課綱已明確規定：W3、W4 必須使用「顏色碰撞法」，W5 必須完成固定砲位的點擊放置與已放置判斷。

---

## 二、總結結論

| 週次 | 目前狀態 | 主要問題 | 建議優先度 |
|---|---|---|---|
| W3 | 大致符合週次目標 | 截圖已埋入另一種「碰到路徑角色」的做法，與課綱的顏色碰撞法不一致 | 高 |
| W4 | 需要修改 | 頁面文字寫顏色碰撞，但實際積木判斷的是 `Sprite1`；抵達後也沒有明確停止程式 | 最高 |
| W5 | 需要修改 | 截圖沒有「如果是蛋才放置」的條件，已放置的母雞仍可再次被點擊執行 | 高 |

建議先統一 W3、W4 的移動技術，再修改 W5。若 W3、W4 的技術方向未先決定，後續 W6 的雞蛋攻擊會建立在不穩定的老鼠程式上。

---

# Week 3：老鼠上路①（直線前進）

## 1. 原本要完成的內容

依 `課綱.md` 第 178 行，W3 的基本任務是：

- 將背景上的路線複製成獨立的路徑角色
- 準備老鼠的紅框、藍框造型
- 設定老鼠的起點、大小與方向
- 建立 `mousemove` 自訂積木
- 讓老鼠用「移動一點」的方式直線前進
- 用重複積木讓老鼠持續移動

W3 不要求轉彎，也不要求抵達飼料桶消失。這些應留到 W4。

## 2. 目前教材內容

目前 `week3/index.md` 已包含：

- 複製背景路線並建立路徑角色
- 將路徑角色定位到 `x: 0、y: 0`
- 加入老鼠
- 準備紅框與藍框造型
- 設定老鼠起點、大小與方向
- 建立 `mousemove`
- 在 `mousemove` 中放入「移動 10 點」
- 用「重複無限次」持續呼叫 `mousemove`

因此，從頁面結構來看，W3 大致符合課綱。

但是目前 W3 附帶的截圖已經使用了 W4 的另一種判斷方式：

- 老鼠切換成藍框或紅框造型
- 判斷是否「碰到 `Sprite1`」
- 再依照目前造型向左或向右轉

這不是課綱所定義的「碰到藍色／紅色」顏色碰撞法。

## 3. 建議修改方式

### 必須修改

W3 的主教材應維持單純，只教直線前進，不要在 W3 主流程放入轉彎判斷。

建議保留以下主流程：

```text
當綠旗被點擊
移到起點
設定大小
面朝起始方向
重複無限次
    mousemove

定義 mousemove
    移動 5～10 點
```

### 需要調整的文字

目前頁面寫「準備紅框、藍框造型（下週判斷用）」可以保留，但要補充：

- 紅框與藍框是 W4 轉彎時使用的方向標記
- W3 不會使用顏色判斷
- W3 的完成標準只有「老鼠能從起點直線前進」

### 需要決定的技術方向

W4 必須改成課綱指定的真正顏色碰撞法，因此 W3 的素材不能再暗示「碰到 `Sprite1`」是正式方法。

目前使用 `Sprite1` 的截圖可以：

- 移到教師備課資料，不放在學生主教材中；或
- 標註為「舊版參考，不使用」；或
- 重新製作成真正的顏色碰撞截圖。

## 4. W3 需要的素材

### 可以保留

- `week3/resources/path/` 的路徑角色建立截圖
- `week3/resources/mouse/加入一隻老鼠.jpg`
- `week3/resources/mouse/把老鼠移到出發地並調整大小和方向.jpg`
- `week3/resources/mouse/調整藍紅老鼠位置5-建立一個積木.jpg`
- `week3/resources/mouse/調整藍紅老鼠位置6-取名叫mousemove.jpg`
- `week3/resources/mouse/調整藍紅老鼠位置7-移動10.jpg`
- `week3/resources/mouse/在老鼠初始化完成後且mousemove完成後加入重覆前進.jpg`

### 建議重新製作

- 一張只包含 W3 程式的完整截圖，不出現轉彎判斷
- 一張清楚顯示老鼠起點與直線前進結果的完成圖
- 一張說明 `mousemove` 是「把往前移動包成一個自訂積木」的圖
- 若保留紅框、藍框造型，需各有一張清楚的造型完成圖

### 不應放在 W3 主流程

- 碰到 `Sprite1` 的判斷
- 左轉、右轉
- 抵達飼料桶
- 隱藏或刪除老鼠

---

# Week 4：老鼠上路②（轉彎與抵達）

## 1. 原本要完成的內容

依 `課綱.md` 第 179 行，W4 的基本任務是：

- 使用紅色、藍色的顏色判斷
- 老鼠碰到指定顏色時轉彎
- 調整移動數值與轉彎數值
- 讓老鼠走完整條路
- 抵達飼料桶後消失
- 自己畫新路線是加分挑戰，不是全班必做

課綱第 46～51 行也明確指定使用：

```text
移動一點 → 碰到藍色／紅色 → 改變方向
```

## 2. 目前教材內容

頁面文字寫的是顏色碰撞法，例如：

- 「碰到藍色就轉彎」
- 「碰到紅色往另一邊轉」
- 「碰到顏色感應自己在不在路上」

但目前實際截圖中的積木是：

- 老鼠切換成藍框造型
- 判斷「碰到 `Sprite1`」
- 向左轉 15 或 20 度
- 老鼠切換成紅框造型
- 再判斷「碰到 `Sprite1`」
- 向右轉 15 或 20 度

也就是說，目前實作是：

```text
老鼠造型決定轉彎方向
碰到路徑角色 Sprite1 才觸發轉彎
```

而不是：

```text
碰到藍色決定轉彎方向
碰到紅色決定轉彎方向
```

這是 W3、W4 目前最重要的不一致。

## 3. 建議修改方式

### 建議採用課綱版本，不繼續使用 Sprite1 碰撞

為了符合新版課綱，建議把 W4 改成真正的顏色碰撞法：

1. 在道路轉角放置清楚的藍色、紅色標記。
2. 老鼠使用 Scratch 的「碰到顏色」感應積木。
3. 碰到藍色時向固定方向轉彎。
4. 碰到紅色時向另一方向轉彎。
5. 每次移動距離與轉彎角度都可以測試調整。

程式概念應接近：

```text
定義 mousemove
    移動 5 點
    如果碰到藍色
        左轉 20 度
    如果碰到紅色
        右轉 20 度
```

實際角度不必固定為 20 度，但教材必須明確說明：數值要依路線測試調整。

### 抵達飼料桶時要停止

目前頁面只寫「碰到飼料桶就隱藏」，但老鼠仍可能留在 `重複無限次` 中繼續執行。

建議改成：

```text
如果碰到飼料桶
    隱藏
    停止這個程式
```

若未來 W8 改成分身，再改為「刪除這個分身」。W4 目前還不是分身，不應提前使用刪除分身。

### W4 頁面順序建議

建議順序如下：

1. 說明顏色標記與碰撞原理
2. 加入藍色判斷
3. 加入紅色判斷
4. 測試並調整移動距離與轉彎角度
5. 加入抵達飼料桶後隱藏並停止
6. 完成檢查
7. 時間足夠才做自己畫路線

目前「自己畫新路線」的位置與課綱一致，可以保留為 PLUS，但必須提醒學生同步更新背景路線與路徑角色。

## 4. W4 需要的素材

### 必須重新製作

- 一張顯示道路轉角有藍色標記的背景圖
- 一張顯示道路轉角有紅色標記的背景圖
- 一張顯示背景道路、路徑角色、顏色標記三者對齊的完成圖
- 一張「碰到藍色」Scratch 感應積木截圖
- 一張「碰到紅色」Scratch 感應積木截圖
- 一張包含完整 `mousemove` 的程式截圖
- 一張顯示「抵達飼料桶 → 隱藏 → 停止這個程式」的截圖
- 一張老鼠成功走完全程的完成畫面

### 目前素材的處理方式

以下素材目前是 `碰到 Sprite1` 的版本，不能直接當成新版課綱的主要教學圖：

- `week4/resources/collision/調整藍紅老鼠位置8-換到藍框造型.jpg`
- `week4/resources/collision/調整藍紅老鼠位置9-加上是否有撞到藍色的判斷和處理.jpg`
- `week4/resources/collision/調整藍紅老鼠位置10-加上是否有撞到紅色的判斷和處理.jpg`
- `week4/resources/collision/調整藍紅老鼠位置11-如果會飛出去可以改變這些數字.jpg`

這些圖可以保留作為舊版備查，但建議不要只修改圖片說明，因為圖片內的積木本身就不是顏色碰撞法。

## 5. W4 的教師起始檔要求

建議準備一個符合新版課綱的 W4 起始檔，至少包含：

- W3 已完成的老鼠直線移動
- 已準備好的紅框、藍框老鼠造型
- 固定路線
- 藍色與紅色轉彎標記
- 路徑角色與背景完全對齊
- 老鼠從正確起點開始

這樣學生可以把時間放在條件判斷與除錯，而不是重新做美術工作。

---

# Week 5：母雞就位（免費放置砲塔）

## 1. 原本要完成的內容

依 `課綱.md` 第 180 行，W5 的基本任務是：

- 使用固定砲位
- 空砲位先顯示為蛋
- 點擊空砲位後變成守衛母雞
- 判斷砲位是否已經放置
- 本週先免費放置，不扣金幣
- 不做自由拖放

## 2. 目前教材內容

頁面文字描述得很完整，包含：

- 母雞造型
- 複製固定砲位
- 蛋作為空砲位
- 點擊空砲位換成母雞
- 使用造型判斷是否已放置
- 本週不扣金幣

目前截圖則是：

```text
當綠旗被點擊
    造型換成 Egg-a

當角色被點擊
    造型換成 Hen-a
    說話
```

截圖沒有「如果目前是蛋」的條件。因此已經變成母雞的砲位仍然可以被點擊，程式會再次執行。

## 3. 建議修改方式

### 建議保留造型作為狀態

這符合 W5 的難度控制，也不需要提早教變數。每個砲位只需要兩個造型：

- `Egg-a`：空砲位
- `Hen-a`：已放置砲塔

### 點擊程式補上條件

教材中的程式應改成：

```text
當綠旗被點擊
    造型換成 Egg-a

當這個角色被點擊
    如果〈造型名稱 = Egg-a〉
        造型換成 Hen-a
        說「我來保護飼料！」持續 2 秒
```

如果 Scratch 版本或介面不方便比較造型名稱，也可以使用固定的造型編號，但教材要清楚標示哪一個編號代表蛋：

```text
如果〈造型編號 = 1〉
    換成 Hen-a
```

優先建議使用造型名稱，學生較容易理解「蛋才是可以放置的狀態」。

### 需要補充測試步驟

W5 應明確要求學生測試兩次：

1. 第一次點擊蛋：蛋變成母雞。
2. 第二次點擊同一個砲位：不應再次放置，也不應重複說話。

這個測試才真正證明「判斷是否已放置」完成。

## 4. W5 需要的素材

### 必須重新製作或補充

- 一張完整的點擊程式截圖，必須看見 `如果〈造型名稱 = Egg-a〉`
- 一張程式與蛋造型對應的說明圖
- 一張第一次點擊前的蛋砲位畫面
- 一張第一次點擊後變成母雞的畫面
- 一張第二次點擊後仍維持母雞、不重複反應的畫面或測試說明
- 一張所有固定砲位完成排列的舞台畫面

### 可以保留

- `week5/resources/turret/選擇母雞造型.jpg`
- `week5/resources/turret/幫母雞加一個新造.jpg`
- `week5/resources/turret/複制HEN做出三個炮位.jpg`
- `week5/resources/turret/點右鍵或是長按複製.jpg`
- `week5/resources/turret/把母雞縮小放到第一個炮位.jpg`
- `week5/resources/turret/加入一個蛋當空的炮位.jpg`

### 需要替換或補拍

`week5/resources/turret/開始是蛋點了變母雞並說出話.jpg` 目前沒有條件判斷，不能直接作為 W5 最後的核心程式截圖。應用加入條件後的新截圖替換，或至少在後面補上一張正確完整的程式圖。

## 5. W5 的教師起始檔要求

建議準備一個 W5 起始檔，包含：

- W4 已能走完全程的老鼠
- 固定道路與飼料桶
- 3～6 個已排列好的固定砲位
- 每個砲位都有 `Egg-a` 與 `Hen-a` 造型
- 每個砲位的綠旗初始化程式

學生只需要完成點擊判斷，不必把時間花在重複複製與對齊所有砲位上。

---

# 三、建議的修改順序

1. 先決定並製作 W4 的真正顏色碰撞範例。
2. 用新的 W4 範例確認老鼠能穩定轉彎並在飼料桶停止。
3. 更新 W3 的說明與截圖，移除或標示 `Sprite1` 舊版碰撞素材。
4. 更新 W4 的所有程式截圖與文字。
5. 製作 W5 的「蛋才可點擊」條件截圖。
6. 用同一份 Scratch 專案實際測試 W3、W4、W5 的銜接。
7. 確認每週下課時都有可操作成果：
   - W3：老鼠能直線前進
   - W4：老鼠能轉彎、走完全程並消失
   - W5：蛋能變母雞，母雞不能重複放置

## 四、修改完成判定

### W3

- [ ] 主教材只教直線前進
- [ ] 老鼠能從固定起點開始移動
- [ ] `mousemove` 能被重複呼叫
- [ ] 沒有把 W4 的轉彎邏輯混入核心步驟

### W4

- [ ] 積木真的使用「碰到藍色／紅色」
- [ ] 不再以 `碰到 Sprite1` 代替顏色判斷
- [ ] 老鼠能依顏色向不同方向轉彎
- [ ] 老鼠抵達飼料桶後隱藏並停止
- [ ] 自己畫路線仍標示為加分挑戰

### W5

- [ ] 空砲位初始造型是蛋
- [ ] 點擊蛋會變成母雞
- [ ] 點擊已放置的母雞不會重複放置
- [ ] 本週沒有加入金幣扣除
- [ ] 固定砲位不需要自由拖放
